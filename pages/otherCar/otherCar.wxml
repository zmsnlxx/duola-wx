<view class="other-car">
  <map id="mapId" class="map" latitude="{{ latitude }}" longitude="{{ longitude }}" markers="{{ markers }}" show-location />
  <view class="content">
    <info-card wx:for="{{ list }}" wx:key="index" info="{{ item }}" />
  </view>
</view>
